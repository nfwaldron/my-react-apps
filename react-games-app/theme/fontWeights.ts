export type ThemeFontWeights = {
    light: number;
    book: number;
    medium: number;
    bold: number;
    black: number;
};

export const fontWeights: ThemeFontWeights = {
    light: 200,
    book: 300,
    medium: 500,
    bold: 700,
    black: 800,
};
